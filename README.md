🧮 Advanced Calculator (Multi-number + Expression Mode)
🔍 Overview

This Calculator is a Python-based command-line tool that allows users to perform both simple arithmetic operations (Add, Subtract, Multiply, Divide) and complex expression evaluations.
It offers two flexible modes:

Simple Mode – Step-by-step calculations (only plain numbers, no symbols).

Expression Mode – Full expression evaluation (e.g., 10 + 20 - 5 * 2 / 5).

This project is ideal for beginners learning Python functions, loops, and error handling.

⚙️ Features

✅ Add, Subtract, Multiply, and Divide multiple numbers
✅ Expression evaluator using Python’s eval()
✅ Input validation (blocks unwanted operators in simple mode)
✅ User-friendly menus and clear visual interface
✅ Handles invalid inputs gracefully
✅ Prevents division by zero
✅ Reusable and modular functions

🧩 Menu System

Main Menu:
1️⃣  Simple Mode (step-by-step input)
2️⃣  Expression Mode (type full equation)
3️⃣  Exit
Simple Mode Menu:
1. ➕ Add multiple numbers
2. ➖ Subtract multiple numbers
3. ✖️ Multiply multiple numbers
4. ➗ Divide multiple numbers
5. 🔙 Back to Main Menu

💡 Example Usage

Addition (Simple Mode):
Enter numbers to add (separated by spaces): 10 20 30
✅ Result: The total sum is 60.0

Expression Mode:
Enter your expression: 10 + 5 * 2 - 4 / 2
✅ Result: 18.0

Invalid Input Handling:
Enter numbers to add: 10 + 20
⚠️ Invalid input '+20' — please enter only plain numbers.
❌ Error! Symbols are not allowed in addition section.
